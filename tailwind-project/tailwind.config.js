// you can add fonts, colors etc
// in terminal, run:
// npx tailwind init // for configuration
// npm run build:css // after adding some code, run again to refresh
module.exports = {
  theme: {
    // is the same as <div class="container mx-auto px-2"> in html file
    container: {
      center: true,
      padding: '2rem',
    },
  },
  variants: {},
  plugins: [],
};
